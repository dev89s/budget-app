<%= render @category %>

<% if (!current_page? categories_path) && (!current_page? root_path) %>
  <%= button_to 'Add Purchase', new_purchase_path, method: 'get', params: { category: @category }, class: "add-purchase" %>
<% end %>

<div>
  <%# <%= link_to "Edit this category", edit_category_path(@category) %>
  <%# <%= link_to "Back to categories", categories_path %>

  <%# <%= button_to "Destroy this category", @category, method: :delete %>
</div>
